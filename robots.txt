# SoGuDiff project page — https://sogudiff.github.io/
#
# An organization site is served from the domain root, so crawlers read this
# file. (A project page at github.io/<repo>/ would serve it under that subpath,
# where it is never fetched.)

User-agent: *
Allow: /

# Crawling 27 MB of demonstration video gains a search engine nothing and costs
# bandwidth; the page itself is what should be indexed.
Disallow: /static/videos/

Sitemap: https://sogudiff.github.io/sitemap.xml
